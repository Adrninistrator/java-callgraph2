## （作用）在 *合并需要解析的目录或jar/war文件时* 使用的表达式语言。若当前配置文件中的表达式执行结果为真，则忽略对应的文件
## （作用）若当前配置文件中的表达式执行结果为假，则处理对应的文件；若未指定表达式，则当前配置不会忽略文件
## （范围）jar/war中的jar文件
## （表达式语言使用示例文件）请参考 el_example.md
## （允许使用的变量）{变量名称} {变量类型} {变量描述} {变量示例}
## {file_path} {String} {jar/war文件中的文件相对路径, 相对根目录的路径, 以斜杠/为分隔符，不以斜杠/开头} {a/b/c.jar, a/b/c.xml}
## {file_name} {String} {文件名称} {a.class, a.xml}
